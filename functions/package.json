{
  "name": "functions",
  "description": "Cloud functions for bedtime-writer",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "lint": "npx prettier --write .",
    "test": "node -r dotenv/config --test",
    "lint_check": "npx prettier --check .",
    "local_backend": "firebase emulators:start",
    "deploy_functions": "firebase deploy --only functions",
    "deploy_hosting": "firebase hosting:channel:deploy preview --expires 7d"
  },
  "main": "index.js",
  "dependencies": {
    "dotenv": "^16.0.3",
    "firebase-admin": "^11.5",
    "firebase-functions": "^4.2",
    "openai": "^3.1.0"
  },
  "devDependencies": {
    "eslint": "^8.34",
    "eslint-config-prettier": "^8.6.0",
    "firebase-functions-test": "^0.2",
    "prettier": "2.8.6"
  },
  "private": true,
  "type": "module"
}
